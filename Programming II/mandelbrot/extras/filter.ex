defmodule Filter do

  def rgb_to_gray() do 
    #fn({:gray, size, depth}) ->
      # {:ok, 1

  end

  def gray_edge() do 
    #fn({:gray, size, depth}) ->
  #{:ok, 3, 
       # {:gray, size, depth},
      # fn(lines -> Kern.fold([0,1,0,1,-4,1,0,1,0]), lines, {0,0,0}, depth) end
    # end
  end
end
